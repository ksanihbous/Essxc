<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= siteConfig.siteName %> - Home</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body class="bg-grid">
<header class="nav">
  <div class="nav-left">
    <img src="/img/logo-exhub.png" class="logo">
    <span class="nav-title"><%= siteConfig.siteName %></span>
  </div>
  <nav class="nav-links">
    <a href="/" class="active">Home</a>
    <a href="/get-key">Get Key</a>
    <a href="/scripts">Scripts</a>
    <% if (user) { %>
      <a href="/dashboard">Dashboard</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
    <% } %>
  </nav>
</header>

<main class="hero">
  <div class="hero-text">
    <h1>Dominate Every Game<br>With <span class="accent"><%= siteConfig.siteName %>.</span></h1>
    <p class="hero-sub">
      Exclusive Roblox scripts with features you'll only find here,
      built for better performance and gameplay experience.
    </p>

    <div class="hero-buttons">
      <a href="<%= siteConfig.discordInvite %>" class="btn btn-discord">
        <img src="/img/logo-discord.png" class="icon-sm">
        Join Community
      </a>
      <a href="/get-key" class="btn btn-outline">Get Key</a>
    </div>

    <div class="hero-badges">
      <span>✔ Undetected &amp; Secure</span>
      <span>✔ Multi-Platform Support</span>
      <span>✔ Instant Updates</span>
    </div>

    <div class="hero-stats">
      <div class="stat">
        <span class="stat-label">Total Keys</span>
        <span class="stat-value"><%= stats.totalKeys %></span>
      </div>
      <div class="stat">
        <span class="stat-label">Active Keys</span>
        <span class="stat-value"><%= stats.activeKeys %></span>
      </div>
    </div>
  </div>

  <div class="hero-code-card">
    <div class="code-header">
      <span class="dot dot-green"></span>
      <span class="code-title">loader.lua</span>
    </div>
    <pre class="code-block">
-- load <%= siteConfig.siteName %> universal loader
loadstring(game:HttpGet(
  "<%= siteConfig.loader.replace(/"/g, '&quot;') %>"
))()</pre>
    <button class="btn btn-copy" data-copy="<%= siteConfig.loader %>">
      Copy Loader
    </button>
  </div>
</main>

<script src="/js/protect.js"></script>
</body>
</html>