<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Scripts - <%= siteName %></title>
  <link rel="stylesheet" href="/public/css/main.css" />
  <script src="/public/js/protect.js" defer></script>
</head>
<body class="page-dark">
<%- include('partials/nav', { active: 'scripts' }) %>

<main class="container">
  <section class="page-header">
    <h1>Supported Games</h1>
    <p>Browse scripts that are available in <%= siteName %>. You can manage the list from the admin dashboard later.</p>
  </section>

  <section class="scripts-grid">
    <% scripts.forEach(function(s) { %>
      <article class="script-card">
        <img src="<%= s.thumbnail %>" alt="<%= s.name %>" class="script-card-thumb" />
        <div class="script-card-body">
          <div class="script-card-header">
            <h2><%= s.name %></h2>
            <span class="script-version"><%= s.version %></span>
          </div>
          <p class="script-desc"><%= s.description %></p>
          <div class="script-meta-row">
            <span class="status-pill <%= s.status === 'working' ? 'status-working' : 'status-maint' %>">
              <%= s.status === 'working' ? 'Working' : 'Maintenance' %>
            </span>
            <% if (s.isFree) { %>
              <span class="badge badge-soft">Free</span>
            <% } %>
          </div>
        </div>
        <div class="script-card-footer">
          <button
            class="btn btn-small btn-primary"
            onclick="navigator.clipboard && navigator.clipboard.writeText('loadstring(game:HttpGet("<%= loaderUrl %>");"
          >
            Copy Loader
          </button>
          <a href="<%= s.gameUrl %>" target="_blank" class="btn btn-small btn-outline">
            Game Link
          </a>
        </div>
      </article>
    <% }) %>
  </section>
</main>

<footer class="footer">
  <span><%= scripts.length %> supported games.</span>
</footer>
</body>
</html>
